import 'package:flutter/material.dart';
class CosmicSettings extends StatelessWidget {
  const CosmicSettings({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('SETTINGS', style: TextStyle(color: Color(0xFF00FF41))));
}
