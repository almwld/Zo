import 'package:flutter/material.dart';
class CosmicMonitor extends StatelessWidget {
  const CosmicMonitor({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('MONITOR', style: TextStyle(color: Color(0xFF00FF41))));
}
