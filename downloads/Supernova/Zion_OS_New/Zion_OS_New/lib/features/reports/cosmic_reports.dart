import 'package:flutter/material.dart';
class CosmicReports extends StatelessWidget {
  const CosmicReports({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('REPORTS', style: TextStyle(color: Color(0xFF00FF41))));
}
