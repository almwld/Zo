import 'package:flutter/material.dart';
class CosmicTerminal extends StatelessWidget {
  const CosmicTerminal({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('TERMINAL', style: TextStyle(color: Color(0xFF00FF41))));
}
