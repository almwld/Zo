import 'package:flutter/material.dart';
class CosmicNetworkMap extends StatelessWidget {
  const CosmicNetworkMap({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('NETWORK MAP', style: TextStyle(color: Color(0xFF00FF41))));
}
